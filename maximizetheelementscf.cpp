#include<iostream>
using namespace std;

int binarySearch(int array[], int x, int low, int high) {
  
	// Repeat until the pointers low and high meet each other
  while (low <= high) {
    int mid = low + (high - low) / 2;

    if (array[mid] == x)
      return mid;

    if (array[mid] < x)
      low = mid + 1;

    else
      high = mid - 1;
  }

  return -1;
}

    


int maxmizeElement(int a[]; int N){
    int temp[100];
    for ( i = 0; i < N; i++)
    {
        int temp[i]==arr[i];

    }
    
     
}

for ( i = 0; i < count; i++)
{
    /* code */
}



int main(){
    int t, a[100];
    cin>>t;
    int n,a,i;
    for ( i = 0; i < n; i++)
    {
       cin>>a[i]; /* code */
    }
    int temp[100];
  
    int maximizeElement(a,N );

    
}

