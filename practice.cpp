
#inlcude<iostream>
using namespace std;
int main(){
    int i;//the iterator 
    cin>>i;                                /*Vectors C++ 
                                            1.)  push_back( thisadds one of the elements to be added in the vector/ put the element that u need to push_back or (push-back) in the vector array )
                                            2.)  */
    int N; //Enter the number of elements
    cin>>N;
    for(i=0;i<N;i++){
        cin>>arr[i];
    }
     vector <int> pairs;

}